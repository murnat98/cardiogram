#pragma once

#include <map>
#include <vector>
#include <iostream>

#ifdef DEBUGMODE

#include <iostream>

#endif // DEBUGMODE

using Pixels = std::vector<std::pair<int, int> >;
using Chars  = std::vector<int>;